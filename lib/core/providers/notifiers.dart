// valureNotifier : hold the data
// valueListenerBuilder : listen to the changes in the valueNotifier (dont need to use setState)
// valueListenerBuilder : rebuild the widget when the value changes
// import 'package:flutter/widgets.dart';

// ValueNotifier<int> selectedPageNotifier = ValueNotifier<int>(0);

// ValueNotifier<bool> isDarkModeNotifier = ValueNotifier<bool>(false);
